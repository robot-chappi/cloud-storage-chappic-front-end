import { NextPage } from 'next'
import PublicFile from '@/app/components/screens/public-file/PublicFile'

const PublicFilePage: NextPage = (props) => {
	return <PublicFile {...props} />
}

export default PublicFilePage
